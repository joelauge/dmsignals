{
  "name": "dmsignals",
  "version": "2.0.0",
  "description": "Cloudflare Worker that monitors Reddit for DM/GM equipment-need signals, drafts replies with Claude Haiku, tracks inbox replies, and emails a daily digest via Resend.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
